# collapse { #genominterv.interval_set_op.collapse }

```python
genominterv.interval_set_op.collapse(a)
```

Converts a list of sorted overlapping intervals to non-overlapping
intervals spanning each inCollapsed non intervals of two sorted 
lists of (start, end) intervals.

## Parameters {.doc-section .doc-section-parameters}

<code>[**a**]{.parameter-name} [:]{.parameter-annotation-sep} []{.parameter-annotation}</code>

:   List of intervals.

## Returns {.doc-section .doc-section-returns}

<code>[]{.parameter-name} [:]{.parameter-annotation-sep} []{.parameter-annotation}</code>

:   List of intervals.